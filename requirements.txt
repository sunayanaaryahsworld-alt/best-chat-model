# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.5

# ── HTTP client (used by openrouter, huggingface, fireworks, ollama) ───────────
httpx>=0.27.0

# ── Environment variable loading ───────────────────────────────────────────────
python-dotenv>=1.0.0

# ── LLM provider SDKs ─────────────────────────────────────────────────────────
groq>=0.9.0                       # Groq

# HuggingFace and Fireworks use plain httpx (no extra SDK needed)
# Ollama uses plain httpx (no extra SDK needed)

firebase-admin